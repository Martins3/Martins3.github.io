分析要求：

生成一份“详细版”总结，不要只写梗概，不要只列几个 bullet 就结束。
正文篇幅通常应达到约 2500 字以上；如果章节本身信息很多，就写得更长，不要为了简短而省略细节。
这个文档写入到 readme.md 中

1. 如果可以 将文章的内容中代码写出来
	- c 语言实现
	- 需要有配套的 makefile ，注意 binary 结尾为 .out ，这样可以自动的被 .gitignore 忽略。
	- 测试需要在本机测试
2. 结合 linux 源码说明本章节的内容，linux kernel 的源码在 ~/data/kernel/linux-build
4. 我关于并发编程的笔记在 ~/data/vn/docs/concurrent ，需要分析当前章节有没有解决我的疑问
